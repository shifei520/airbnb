const initialStete = {
	currentPage: 0,
	roomList: [],
	totalCount: 0,
};

const entireReducer = (state = initialStete, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default entireReducer;
